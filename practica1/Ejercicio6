#Modifique el ejercicio 4 para que dada la lista 
#de número genere dos nuevas listas, 
# una con los número pares y otras 
# con los que son impares. Imprima
#las listas al terminar de procesarlas

lista_par = []
lista_impar = []
lista_numeros = [0,1,2,3,4,5,6,7,8]
for n in lista_numeros:
    if n % 2 == 0:
        lista_par.append(n)
    else:
        lista_impar.append(n)
        
print(lista_par)
print(lista_impar)
print(type(lista_impar))

